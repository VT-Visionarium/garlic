This being kept in a git repo keeps a common /etc/skel files.
/etc/skel/ has in it the file that are copied to /home/user/
when a user is created.

Note: the /etc/adduser.conf file that we use is installed from
../ldap_client/


See 'man adduser'.


FILES:

    xubuntu-10.04/

        For /etc/skel/ on a xubuntu-10.04 system.


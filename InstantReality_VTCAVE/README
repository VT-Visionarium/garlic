This depends on ../instantreality/ and ../diverse/dtk*
apt-get install xmlstarlet

git clone https://github.com/lanceman2/InstantReality_VTCAVE.git git

In the VT CAVE we have no head monitor on the main graphics render
computer so we must ssh to it.  Note remember to use 'ssh -X nodename'.


FILES:

   InstantReality_VTCAVE-VERSION/

        particular version build directory

   common.bash

        used by all install.bash scripts

   git/

        git clone of the source

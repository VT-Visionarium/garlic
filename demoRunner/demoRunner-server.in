#!/bin/bash

set -x

scriptdir="$(dirname ${BASH_SOURCE[0]})" || exit $?
cd "$scriptdir" || exit $?
scriptdir="$PWD" # now we have full path

cd "@WWW_ROOT@/docRoot" || exit $?

exec php -t "@WWW_ROOT@/docRoot" -c "$scriptdir/php.ini" -S localhost:8000

